import SignInForm from "../../components/SignInForm.js"

export const meta = {
    author: "John"
}

<h1 alaaign="center">How to create a sign in form</h1>
When creating a sign in form it's important to use ```<form />``` for processing the input fields inside. Then add 2 inputs. 1 for username/email and 1 for password.

<SignInForm/>